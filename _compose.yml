version: '3'

services:
    api:
        build : Dockerfile
        conrainer_name: lineapi
        command:
        environment:
            - lineapi=
            - url=
    db:
        image: postgres:14
        container_name: postgres
        ports:
            - 5432:5432
        volumes:
            - ./db-store:/var/lib/postgresql/data

volumes:
